# Go release-contract projection

This package is a downstream projection of the parity-approved Contract IR. It provides strict
JSON object decoding and closed-enum validation, but it does not replace the independently
authored TypeSpec or JSON Schema authorities.

Run `go test ./...` from this directory.
