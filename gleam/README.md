# Gleam release-contract projection

This JavaScript-target Gleam package contains immutable custom types and explicit wire-value
functions for the parity-approved WASM release Contract IR. It does not replace the independently
authored TypeSpec or JSON Schema sources.

The CI gate runs `gleam format --check` and `gleam check` with an exact compiler version.
