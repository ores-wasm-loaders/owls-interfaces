# Repository instructions
Interfaces contain contracts, serialized data types and conformance fixtures only.
Runtime policy, I/O, validation engines and activation belong to the host libraries.
JSON Schema and TypeSpec are peer declarations; neither is generated from the other.
Use the shared conformance corpus when changing the wire contract.
Read ~/codes/AGENTS.md or .ores/agents/AGENTS.md when available. Never commit .ores/.

